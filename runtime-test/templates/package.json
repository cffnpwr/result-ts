{
  "name": "runtime-test-for-${runtime}",
  "description": "Runtime test for ${Runtime}",
  "scripts": {
    "test": "vitest --run"
  },
  "devDependencies": {
    "@cffnpwr/result-ts": "${node-only:link:}../../dist",
    "vitest": "4.1.5"
  }
}
